1. my_model 폴더 안에 모델이랑 min&max scaling에 대한 정보 포함
- 원래 scaling은 sklearn 가지고 했었는데, 그거 통과시키는 것보다 그냥 numpy에서 바로 계산하는 게 더 빠를 것 같아서 변경
- model은 keras 파일로 작성되어 있음
- 이전에 초안 때는 RNN은 따로 안 했었는데, 리뷰 오기를 vanilla RNN이랑도 비교해달라고 해서... 그거 비교한 결과도 있습니다
- 근데 문제는 RNN 결과가 더 좋게 나왔어서... 일단 논문 최종본에는 안 싣기는 했는데 혹시 몰라서 같이 드려요 ㅎㅎ...

2. 모델 불러오는 방법은 main_compare_250310.py 참고
- 그냥 keras 모델 불러와서 model.predict 해도 되기는 하는데
- 이것도 위와 마찬가지의 이유로 그냥 함수화해서 만들어주는 게 빠를 것 같아서 + 매트랩 옮기기 전 테스트 용으로 weight 뽑아서 함수화했스빈다
- 함수화 하는 방법은 main_compare_250310.py의 lstm_fcn 함수 참고 (augmented matrix의 gate 순서가 알고 있는 순서랑 약간 다를 거라서..ㅎㅎ)

3. 데이터는 여기에 따로 안 넣어 놨는데, main_compare_250310.py 폴더 경로에 맞게 알아서 뽑은 데이터 집어넣고 사용해보세요!